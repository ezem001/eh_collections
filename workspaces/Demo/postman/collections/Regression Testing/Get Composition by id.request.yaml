$kind: http-request
url: "{{host}}/api/patients/{{patient_for_regress}}/compositions/{{JobCompositi\
  onID}}"
method: GET
headers:
  Content-Type: application/json
  Authorization: Bearer {{access_token}}
  api-key: "{{mis_client_secret}}"
  X-Custom-PSK: "{{x-custom-psk}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Get Composition by id - PASS. Status code is 200\", function ()
      {\r

      \    pm.response.to.have.status(200);\r

      });"
    language: text/javascript
order: 93000
