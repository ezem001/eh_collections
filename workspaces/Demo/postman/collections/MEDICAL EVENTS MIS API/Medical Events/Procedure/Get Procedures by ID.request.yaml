$kind: http-request
url: "{{host}}/api/patients/{{patient_id_from_episode}}/procedures/{{jobProcedu\
  reID}}"
method: GET
headers:
  Content-Type: application/json
  api-key: "{{mis_client_secret}}"
  Authorization: Bearer {{access_token}}
  X-Custom-PSK: "{{x-custom-psk}}"
order: 4000
