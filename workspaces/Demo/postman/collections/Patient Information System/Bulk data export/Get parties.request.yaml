$kind: http-request
url: "{{pis_host}}/api/pis/bulk/parties"
method: GET
headers:
  X-Custom-PSK: "{{x-custom-psk}}"
  Content-Type: application/json
  api-key: "{{client_secret}}"
queryParams:
  - key: depth
    value: "1"
    disabled: true
order: 1000
