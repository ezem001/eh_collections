$kind: http-request
url: "{{host}}/api/patients/{{patient_for_regress}}/episodes/{{jobEpisodeID}}?l\
  egal_entity={{user_legal_entity}}"
method: GET
headers:
  Content-Type: application/json
  Authorization: Bearer {{access_token}}
  api-key: "{{mis_client_secret}}"
  X-Custom-PSK: "{{x-custom-psk}}"
queryParams:
  legal_entity: "{{user_legal_entity}}"
body:
  type: text
  content: ""
scripts:
  - type: afterResponse
    code: |-
      pm.test("Get Episode by ID - PASS. Status code is 200", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 15000
